{% extends 'base.html.twig' %}

{% block content %}
    <h1>Inscription</h1>
    {% if notification %}
        <div class="alert-info">
            {{ notification }}
        </div>
    {% endif %}
    {{ form(form) }}
{% endblock%}

